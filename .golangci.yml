version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: standard
  enable:
    - misspell
    - revive
    - goimports
    - gofmt
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
